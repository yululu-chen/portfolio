<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Dispatch — 会议行动项分发器</title>
  <style>
    :root {
      color-scheme: light;
      --bg: #f7f7f5;
      --surface: #ffffff;
      --border: #e8e8e4;
      --border-strong: #d0d0ca;
      --text-primary: #0f0f0e;
      --text-secondary: #6b6b63;
      --text-muted: #a8a89e;
      --blue: #1a6dff;
      --blue-bg: #eef4ff;
      --blue-border: #c3d9ff;
      --yellow: #d97706;
      --yellow-bg: #fffbeb;
      --yellow-border: #fde68a;
      --green: #16a34a;
      --green-bg: #f0fdf4;
      --green-border: #bbf7d0;
      --red: #dc2626;
      --red-bg: #fef2f2;
      --tag-bg: #eeeee8;
      --tag-text: #5c5c54;
      --radius: 8px;
      --radius-sm: 5px;
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      background: var(--bg);
      color: var(--text-primary);
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Hiragino Sans GB", sans-serif;
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
      min-height: 100vh;
    }

    /* ── top bar ── */
    .topbar {
      background: var(--surface);
      border-bottom: 1px solid var(--border);
      padding: 0 24px;
      height: 52px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      position: sticky;
      top: 0;
      z-index: 100;
    }

    .topbar-logo {
      display: flex;
      align-items: center;
      gap: 8px;
      font-weight: 700;
      font-size: 15px;
      letter-spacing: -0.02em;
    }

    .topbar-logo-dot {
      width: 7px;
      height: 7px;
      background: var(--blue);
      border-radius: 50%;
    }

    .topbar-steps {
      display: flex;
      align-items: center;
      gap: 4px;
    }

    .step-pill {
      font-size: 11px;
      padding: 3px 10px;
      border-radius: 20px;
      color: var(--text-muted);
      background: var(--tag-bg);
      cursor: pointer;
      transition: all 0.15s;
      user-select: none;
    }

    .step-pill.active {
      background: var(--blue);
      color: #fff;
    }

    .step-sep {
      font-size: 10px;
      color: var(--border-strong);
    }

    /* ── screens ── */
    .screen {
      display: none;
      max-width: 860px;
      margin: 0 auto;
      padding: 40px 24px 80px;
    }

    .screen.active { display: block; }

    /* ════════════════════════════
       SCREEN 1 — 输入区
    ════════════════════════════ */
    .s1-header {
      margin-bottom: 32px;
    }

    .s1-eyebrow {
      font-size: 11px;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--text-muted);
      margin-bottom: 10px;
    }

    .s1-title {
      font-size: 26px;
      font-weight: 700;
      letter-spacing: -0.03em;
      margin-bottom: 8px;
    }

    .s1-subtitle {
      font-size: 14px;
      color: var(--text-secondary);
    }

    .demo-chips {
      display: flex;
      gap: 8px;
      margin-bottom: 16px;
      flex-wrap: wrap;
    }

    .demo-chip {
      font-size: 12px;
      padding: 5px 12px;
      border: 1px solid var(--border-strong);
      border-radius: 20px;
      cursor: pointer;
      background: var(--surface);
      color: var(--text-secondary);
      transition: all 0.15s;
      user-select: none;
    }

    .demo-chip:hover, .demo-chip.active {
      border-color: var(--blue);
      color: var(--blue);
      background: var(--blue-bg);
    }

    .transcript-box {
      width: 100%;
      min-height: 220px;
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 16px;
      font-size: 13px;
      color: var(--text-secondary);
      background: var(--surface);
      resize: vertical;
      outline: none;
      line-height: 1.7;
      font-family: inherit;
      transition: border-color 0.15s;
    }

    .transcript-box:focus {
      border-color: var(--blue);
    }

    .s1-actions {
      margin-top: 20px;
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .btn-primary {
      background: var(--text-primary);
      color: #fff;
      border: none;
      padding: 10px 24px;
      border-radius: var(--radius-sm);
      font-size: 13px;
      font-weight: 600;
      cursor: pointer;
      transition: opacity 0.15s;
      font-family: inherit;
    }

    .btn-primary:hover { opacity: 0.82; }

    .btn-ghost {
      background: none;
      border: 1px solid var(--border-strong);
      color: var(--text-secondary);
      padding: 10px 18px;
      border-radius: var(--radius-sm);
      font-size: 13px;
      cursor: pointer;
      transition: all 0.15s;
      font-family: inherit;
    }

    .btn-ghost:hover {
      border-color: var(--text-secondary);
      color: var(--text-primary);
    }

    .extract-hint {
      font-size: 12px;
      color: var(--text-muted);
    }

    /* AI 提取动画 */
    .extracting-overlay {
      display: none;
      margin-top: 24px;
      padding: 20px;
      border: 1px solid var(--border);
      border-radius: var(--radius);
      background: var(--surface);
      text-align: center;
    }

    .extracting-overlay.show { display: block; }

    .extracting-dots {
      display: inline-flex;
      gap: 5px;
      margin-bottom: 10px;
    }

    .extracting-dots span {
      width: 6px;
      height: 6px;
      border-radius: 50%;
      background: var(--blue);
      animation: bounce 1.2s infinite;
    }

    .extracting-dots span:nth-child(2) { animation-delay: 0.2s; }
    .extracting-dots span:nth-child(3) { animation-delay: 0.4s; }

    @keyframes bounce {
      0%, 80%, 100% { transform: scale(0.6); opacity: 0.4; }
      40% { transform: scale(1); opacity: 1; }
    }

    .extracting-text {
      font-size: 13px;
      color: var(--text-secondary);
    }

    /* ════════════════════════════
       SCREEN 2 — 确认面板
    ════════════════════════════ */
    .s2-header {
      display: flex;
      align-items: flex-start;
      justify-content: space-between;
      margin-bottom: 28px;
      gap: 16px;
      flex-wrap: wrap;
    }

    .s2-title-block {}

    .s2-title {
      font-size: 20px;
      font-weight: 700;
      letter-spacing: -0.02em;
      margin-bottom: 4px;
    }

    .s2-subtitle {
      font-size: 13px;
      color: var(--text-secondary);
    }

    .s2-toolbar {
      display: flex;
      gap: 8px;
      align-items: center;
      flex-shrink: 0;
    }

    .confirm-all-btn {
      display: flex;
      align-items: center;
      gap: 6px;
      font-size: 12px;
      font-weight: 600;
      padding: 7px 14px;
      background: var(--green-bg);
      border: 1px solid var(--green-border);
      color: var(--green);
      border-radius: var(--radius-sm);
      cursor: pointer;
      transition: all 0.15s;
      font-family: inherit;
    }

    .confirm-all-btn:hover {
      background: #dcfce7;
    }

    .retry-btn {
      display: flex;
      align-items: center;
      gap: 6px;
      font-size: 12px;
      padding: 7px 14px;
      background: var(--surface);
      border: 1px solid var(--border-strong);
      color: var(--text-secondary);
      border-radius: var(--radius-sm);
      cursor: pointer;
      transition: all 0.15s;
      font-family: inherit;
    }

    .retry-btn:hover {
      border-color: var(--text-secondary);
      color: var(--text-primary);
    }

    /* 行动项卡片 */
    .action-list {
      display: flex;
      flex-direction: column;
      gap: 12px;
    }

    .action-card {
      background: var(--surface);
      border: 1.5px solid var(--border);
      border-radius: var(--radius);
      overflow: hidden;
      transition: border-color 0.2s, box-shadow 0.2s;
    }

    .action-card.status-pending {
      border-color: var(--yellow-border);
    }

    .action-card.status-confirmed {
      border-color: var(--green-border);
    }

    .action-card.status-deleted {
      opacity: 0.4;
      pointer-events: none;
    }

    .action-card-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 14px 16px 10px;
      gap: 12px;
    }

    .action-card-left {
      display: flex;
      align-items: flex-start;
      gap: 10px;
      flex: 1;
    }

    .action-num {
      font-size: 11px;
      color: var(--text-muted);
      background: var(--tag-bg);
      padding: 2px 7px;
      border-radius: 3px;
      flex-shrink: 0;
      margin-top: 2px;
      font-variant-numeric: tabular-nums;
    }

    .action-text {
      font-size: 14px;
      font-weight: 500;
      line-height: 1.5;
      flex: 1;
    }

    .status-badge {
      font-size: 10px;
      font-weight: 600;
      padding: 2px 8px;
      border-radius: 3px;
      flex-shrink: 0;
      letter-spacing: 0.04em;
    }

    .status-badge.pending {
      background: var(--yellow-bg);
      color: var(--yellow);
      border: 1px solid var(--yellow-border);
    }

    .status-badge.confirmed {
      background: var(--green-bg);
      color: var(--green);
      border: 1px solid var(--green-border);
    }

    /* 分发目标选择器 */
    .dispatch-selector {
      padding: 0 16px 12px;
    }

    .dispatch-label {
      font-size: 11px;
      color: var(--text-muted);
      margin-bottom: 8px;
    }

    .dispatch-tabs {
      display: flex;
      gap: 6px;
      margin-bottom: 12px;
      flex-wrap: wrap;
    }

    .dispatch-tab {
      font-size: 12px;
      padding: 5px 12px;
      border: 1px solid var(--border-strong);
      border-radius: 20px;
      cursor: pointer;
      background: var(--bg);
      color: var(--text-secondary);
      transition: all 0.15s;
      user-select: none;
    }

    .dispatch-tab.active {
      background: var(--blue-bg);
      border-color: var(--blue-border);
      color: var(--blue);
    }

    /* 预览块 */
    .preview-block {
      background: var(--bg);
      border: 1px solid var(--border);
      border-radius: var(--radius-sm);
      padding: 12px 14px;
      margin-bottom: 12px;
    }

    .preview-block-title {
      font-size: 11px;
      color: var(--text-muted);
      margin-bottom: 8px;
      letter-spacing: 0.06em;
      text-transform: uppercase;
    }

    .preview-content {
      font-size: 12px;
      color: var(--text-secondary);
      line-height: 1.7;
    }

    .preview-content .pv-line {
      display: flex;
      gap: 6px;
      margin-bottom: 3px;
    }

    .preview-content .pv-key {
      color: var(--text-muted);
      min-width: 52px;
      flex-shrink: 0;
    }

    .preview-content .pv-val {
      color: var(--text-primary);
      font-weight: 500;
    }

    /* 操作按钮行 */
    .action-btns {
      display: flex;
      gap: 6px;
      padding: 0 16px 14px;
      flex-wrap: wrap;
    }

    .btn-sm {
      font-size: 11px;
      padding: 5px 11px;
      border-radius: var(--radius-sm);
      cursor: pointer;
      border: 1px solid var(--border-strong);
      background: var(--surface);
      color: var(--text-secondary);
      transition: all 0.15s;
      font-family: inherit;
    }

    .btn-sm:hover {
      border-color: var(--text-secondary);
      color: var(--text-primary);
    }

    .btn-sm.confirm {
      background: var(--green-bg);
      border-color: var(--green-border);
      color: var(--green);
      font-weight: 600;
    }

    .btn-sm.confirm:hover {
      background: #dcfce7;
    }

    .btn-sm.delete {
      color: var(--red);
      border-color: #fecaca;
    }

    .btn-sm.delete:hover {
      background: var(--red-bg);
    }

    /* 行动项已确认遮罩 */
    .confirmed-overlay {
      display: none;
      padding: 12px 16px;
      background: var(--green-bg);
      border-top: 1px solid var(--green-border);
      font-size: 12px;
      color: var(--green);
      font-weight: 500;
    }

    .action-card.status-confirmed .confirmed-overlay {
      display: block;
    }

    .action-card.status-confirmed .dispatch-selector,
    .action-card.status-confirmed .action-btns {
      display: none;
    }

    /* 修改模态 */
    .edit-modal {
      display: none;
      padding: 12px 16px;
      background: var(--blue-bg);
      border-top: 1px solid var(--blue-border);
    }

    .edit-modal.show { display: block; }

    .edit-modal textarea {
      width: 100%;
      border: 1px solid var(--blue-border);
      border-radius: var(--radius-sm);
      padding: 8px 10px;
      font-size: 13px;
      font-family: inherit;
      line-height: 1.5;
      resize: none;
      outline: none;
      background: var(--surface);
      margin-bottom: 8px;
    }

    .edit-modal-actions {
      display: flex;
      gap: 6px;
    }

    /* 底部分发按钮 */
    .dispatch-bottom {
      margin-top: 24px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 16px 20px;
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      gap: 16px;
      flex-wrap: wrap;
    }

    .dispatch-count {
      font-size: 13px;
      color: var(--text-secondary);
    }

    .dispatch-count strong {
      color: var(--text-primary);
      font-weight: 700;
    }

    .btn-dispatch {
      display: flex;
      align-items: center;
      gap: 8px;
      background: var(--blue);
      color: #fff;
      border: none;
      padding: 11px 24px;
      border-radius: var(--radius-sm);
      font-size: 13px;
      font-weight: 600;
      cursor: pointer;
      transition: opacity 0.15s;
      font-family: inherit;
    }

    .btn-dispatch:hover { opacity: 0.88; }
    .btn-dispatch:disabled { opacity: 0.4; cursor: not-allowed; }

    /* ════════════════════════════
       SCREEN 3 — 分发结果
    ════════════════════════════ */
    .s3-header {
      text-align: center;
      padding: 40px 0 36px;
      border-bottom: 1px solid var(--border);
      margin-bottom: 32px;
    }

    .s3-check {
      width: 48px;
      height: 48px;
      background: var(--green-bg);
      border: 2px solid var(--green-border);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 16px;
      font-size: 20px;
    }

    .s3-title {
      font-size: 22px;
      font-weight: 700;
      letter-spacing: -0.02em;
      margin-bottom: 6px;
    }

    .s3-subtitle {
      font-size: 14px;
      color: var(--text-secondary);
    }

    .result-list {
      display: flex;
      flex-direction: column;
      gap: 0;
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      overflow: hidden;
      margin-bottom: 24px;
    }

    .result-item {
      display: flex;
      align-items: center;
      gap: 14px;
      padding: 16px 20px;
      border-bottom: 1px solid var(--border);
    }

    .result-item:last-child { border-bottom: none; }

    .result-icon {
      font-size: 18px;
      flex-shrink: 0;
    }

    .result-body {
      flex: 1;
    }

    .result-action {
      font-size: 13px;
      font-weight: 500;
      margin-bottom: 2px;
    }

    .result-dest {
      font-size: 12px;
      color: var(--text-muted);
    }

    .result-status {
      font-size: 11px;
      font-weight: 600;
      color: var(--green);
      background: var(--green-bg);
      border: 1px solid var(--green-border);
      padding: 2px 8px;
      border-radius: 3px;
      flex-shrink: 0;
    }

    .s3-actions {
      display: flex;
      gap: 10px;
      justify-content: center;
      flex-wrap: wrap;
    }

    /* 进入动画 */
    @keyframes fadeSlideIn {
      from { opacity: 0; transform: translateY(12px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .screen.active {
      animation: fadeSlideIn 0.25s ease;
    }

    /* 确认动画 */
    @keyframes confirmPulse {
      0% { transform: scale(1); }
      50% { transform: scale(1.02); }
      100% { transform: scale(1); }
    }

    .action-card.just-confirmed {
      animation: confirmPulse 0.3s ease;
    }

    /* 说明栏 */
    .design-note {
      margin-top: 36px;
      padding: 16px 20px;
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      display: flex;
      gap: 12px;
    }

    .design-note-icon {
      font-size: 14px;
      flex-shrink: 0;
      margin-top: 2px;
    }

    .design-note-text {
      font-size: 12px;
      color: var(--text-secondary);
      line-height: 1.7;
    }

    .design-note-text strong {
      color: var(--text-primary);
    }
  </style>
</head>
<body>

  <!-- ── Top bar ── -->
  <div class="topbar">
    <div class="topbar-logo">
      <div class="topbar-logo-dot"></div>
      Dispatch
    </div>
    <div class="topbar-steps">
      <span class="step-pill active" id="pill-1" onclick="goScreen(1)">① 输入转录</span>
      <span class="step-sep">›</span>
      <span class="step-pill" id="pill-2" onclick="goScreen(2)">② 确认行动项</span>
      <span class="step-sep">›</span>
      <span class="step-pill" id="pill-3" onclick="goScreen(3)">③ 分发完成</span>
    </div>
  </div>

  <!-- ══════════════════════════
       SCREEN 1 — 输入
  ══════════════════════════ -->
  <div class="screen active" id="screen-1">
    <div class="s1-header">
      <div class="s1-eyebrow">Step 1 / 3</div>
      <div class="s1-title">粘贴会议转录</div>
      <div class="s1-subtitle">AI 会提取出每一条行动项，等你确认后再分发——不是 AI 替你做决定，是 AI 把决定送到你面前。</div>
    </div>

    <div class="demo-chips">
      <span class="demo-chip active" id="chip-0" onclick="loadDemo(0)">示例 1：产品周会</span>
      <span class="demo-chip" id="chip-1" onclick="loadDemo(1)">示例 2：客户访谈</span>
      <span class="demo-chip" id="chip-2" onclick="loadDemo(2)">示例 3：跨部门对齐</span>
    </div>

    <textarea class="transcript-box" id="transcript" placeholder="粘贴会议转录文本……"></textarea>

    <div class="s1-actions">
      <button class="btn-primary" onclick="startExtract()">AI 提取行动项 →</button>
      <span class="extract-hint">提取后可逐条确认、修改、删除</span>
    </div>

    <div class="extracting-overlay" id="extracting">
      <div class="extracting-dots">
        <span></span><span></span><span></span>
      </div>
      <div class="extracting-text">正在分析转录内容，提取行动项…</div>
    </div>

    <div class="design-note">
      <div class="design-note-icon">ⓘ</div>
      <div class="design-note-text"><strong>设计理念：</strong>行动项默认"待确认"状态——这是信任设计的体现。AI 帮你看到了什么，但最终拍板的是你。确认后才真正分发，避免 AI 误判直接造成混乱。</div>
    </div>
  </div>

  <!-- ══════════════════════════
       SCREEN 2 — 确认面板
  ══════════════════════════ -->
  <div class="screen" id="screen-2">
    <div class="s2-header">
      <div class="s2-title-block">
        <div class="s2-title">确认行动项</div>
        <div class="s2-subtitle" id="s2-subtitle">AI 提取了 3 条行动项，逐条确认分发目标</div>
      </div>
      <div class="s2-toolbar">
        <button class="confirm-all-btn" onclick="confirmAll()">✓ 全部确认</button>
        <button class="retry-btn" onclick="goScreen(1)">↩ 重新提取</button>
      </div>
    </div>

    <div class="action-list" id="action-list"></div>

    <div class="dispatch-bottom">
      <div class="dispatch-count" id="dispatch-count">已确认 <strong>0</strong> / 3 条</div>
      <button class="btn-dispatch" id="dispatch-btn" onclick="doDispatch()" disabled>🚀 一键分发已确认项</button>
    </div>
  </div>

  <!-- ══════════════════════════
       SCREEN 3 — 分发结果
  ══════════════════════════ -->
  <div class="screen" id="screen-3">
    <div class="s3-header">
      <div class="s3-check">✓</div>
      <div class="s3-title">分发完成</div>
      <div class="s3-subtitle" id="s3-subtitle">3 条行动项已成功分发</div>
    </div>

    <div class="result-list" id="result-list"></div>

    <div class="s3-actions">
      <button class="btn-ghost" onclick="resetAll()">开始新会议</button>
      <button class="btn-primary" onclick="goScreen(2)">← 查看行动项详情</button>
    </div>

    <div class="design-note" style="margin-top:24px;">
      <div class="design-note-icon">ⓘ</div>
      <div class="design-note-text"><strong>注：</strong>这是一个交互原型，分发动作均为模拟，并未真实对接飞书 / 日历 API。展示的是产品交互逻辑与信任机制的设计思路。</div>
    </div>
  </div>

<script>
// ══ 预设 Demo 数据 ══
const DEMOS = [
  {
    label: '产品周会',
    transcript: `张三：本周 AI 质检覆盖率到了 85%，高压线违规已全量覆盖，但电访那边还有几条规则没跑通。
李四：我这边客户访谈回来了，有几个反馈需要同步给设计。
王五：目前系统性能有点问题，并发一高延迟就上来了，需要排期优化一下。
主持人：好，总结一下——张三你本周五前出一份质检覆盖率周报，数据要细化到各部门；李四你把客户反馈整理成文档，下周一前同步给产品和设计；王五你约一下后端同学，这周内把性能问题排期拉出来。还有，下次周会时间改到周四上午 10 点，麻烦大家更新一下日历。`,
    actions: [
      {
        id: 1,
        text: '张三周五前出质检覆盖率周报，数据细化到各部门',
        owner: '张三',
        deadline: '本周五',
        defaultTarget: 'doc',
        targets: {
          msg: { icon: '💬', label: '飞书消息', lines: [['接收人', '@张三'], ['内容', '请于本周五前提交质检覆盖率周报，数据需细化到各部门'], ['发送时间', '立即']] },
          cal: { icon: '📅', label: '日历事件', lines: [['事件', '质检周报提交'], ['负责人', '张三'], ['截止', '本周五 18:00'], ['说明', '覆盖率数据细化到各部门']] },
          doc: { icon: '📄', label: '文档草稿', lines: [['文档标题', '质检覆盖率周报 W26'], ['负责人', '张三'], ['截止', '本周五'], ['模板', '本周覆盖率 85%，高压线全量...']] }
        }
      },
      {
        id: 2,
        text: '李四下周一前整理客户反馈文档，同步产品和设计',
        owner: '李四',
        deadline: '下周一',
        defaultTarget: 'doc',
        targets: {
          msg: { icon: '💬', label: '飞书消息', lines: [['接收人', '@李四'], ['内容', '请于下周一前整理客户访谈反馈文档，并同步产品和设计'], ['发送时间', '立即']] },
          cal: { icon: '📅', label: '日历事件', lines: [['事件', '客户反馈文档同步'], ['参与者', '李四、产品、设计'], ['截止', '下周一'], ['提醒', '周日晚 20:00']] },
          doc: { icon: '📄', label: '文档草稿', lines: [['文档标题', '客户访谈反馈汇总'], ['负责人', '李四'], ['同步对象', '产品、设计'], ['截止', '下周一']] }
        }
      },
      {
        id: 3,
        text: '王五本周内约后端同学拉出性能优化排期',
        owner: '王五',
        deadline: '本周内',
        defaultTarget: 'cal',
        targets: {
          msg: { icon: '💬', label: '飞书消息', lines: [['接收人', '@王五'], ['内容', '请本周内约后端同学拉出系统性能优化排期'], ['发送时间', '立即']] },
          cal: { icon: '📅', label: '日历事件', lines: [['事件', '性能优化排期对齐'], ['参与者', '王五 + 后端同学'], ['建议时间', '本周三或四'], ['议题', '高并发延迟问题排期']] },
          doc: { icon: '📄', label: '文档草稿', lines: [['文档标题', '性能优化排期计划'], ['负责人', '王五'], ['截止', '本周内'], ['背景', '高并发时延迟上升']] }
        }
      },
      {
        id: 4,
        text: '全员更新下周周会时间为周四上午 10 点',
        owner: '全员',
        deadline: '今天',
        defaultTarget: 'cal',
        targets: {
          msg: { icon: '💬', label: '飞书消息', lines: [['接收人', '全员群'], ['内容', '通知：周会时间改为每周四 10:00，请大家更新日历'], ['发送时间', '立即']] },
          cal: { icon: '📅', label: '日历事件', lines: [['事件', '产品周会（调整后）'], ['时间', '每周四 10:00'], ['参与者', '全员'], ['类型', '循环会议']] },
          doc: { icon: '📄', label: '文档草稿', lines: [['文档标题', '周会时间变更通知'], ['变更', '原周三 → 周四 10:00'], ['生效', '下周起']] }
        }
      }
    ]
  },
  {
    label: '客户访谈',
    transcript: `产品：刚才和客户聊了一个小时，主要反馈是——搜索功能太慢了，输入关键词要等两三秒才出结果，这个体验很差。
设计：另外他们提到界面上有些按钮不知道是干什么用的，特别是"高级筛选"那里，有用户以为是收费功能不敢点。
产品：对，还有一个问题，客户希望能把自己常用的筛选条件保存下来，每次不用重新配置。
销售：我这边有他们下个月续费的事情，需要在两周内跟进一次。
主持人：行，总结一下——产品你整理一份用研反馈文档，本周五前发给设计和研发；研发这边搜索性能问题优先级拉高，两周内给个方案；设计下周三前出一版"高级筛选"的新交互方案；销售两周内约客户续费沟通。`,
    actions: [
      {
        id: 1,
        text: '产品本周五前整理用研反馈文档，发给设计和研发',
        owner: '产品',
        deadline: '本周五',
        defaultTarget: 'doc',
        targets: {
          msg: { icon: '💬', label: '飞书消息', lines: [['接收人', '@产品'], ['内容', '请于本周五前整理用研反馈文档，同步设计和研发'], ['发送时间', '立即']] },
          cal: { icon: '📅', label: '日历事件', lines: [['事件', '用研反馈文档同步'], ['负责人', '产品'], ['截止', '本周五'], ['同步对象', '设计、研发']] },
          doc: { icon: '📄', label: '文档草稿', lines: [['文档标题', '客户访谈反馈 — 搜索 & 筛选'], ['负责人', '产品'], ['截止', '本周五'], ['核心问题', '搜索慢、筛选交互混乱、无法保存配置']] }
        }
      },
      {
        id: 2,
        text: '研发两周内给出搜索性能优化方案',
        owner: '研发',
        deadline: '两周内',
        defaultTarget: 'doc',
        targets: {
          msg: { icon: '💬', label: '飞书消息', lines: [['接收人', '@研发负责人'], ['内容', '搜索性能问题优先级提升，请两周内给出优化方案'], ['发送时间', '立即']] },
          cal: { icon: '📅', label: '日历事件', lines: [['事件', '搜索性能方案评审'], ['负责人', '研发'], ['截止', '两周内'], ['优先级', 'P1']] },
          doc: { icon: '📄', label: '文档草稿', lines: [['文档标题', '搜索性能优化方案'], ['背景', '关键词搜索延迟 2-3 秒'], ['目标', '< 500ms'], ['截止', '两周内']] }
        }
      },
      {
        id: 3,
        text: '设计下周三前出"高级筛选"新交互方案',
        owner: '设计',
        deadline: '下周三',
        defaultTarget: 'cal',
        targets: {
          msg: { icon: '💬', label: '飞书消息', lines: [['接收人', '@设计'], ['内容', '高级筛选交互需优化，下周三前出新方案，需解决按钮识别度问题'], ['发送时间', '立即']] },
          cal: { icon: '📅', label: '日历事件', lines: [['事件', '高级筛选交互方案评审'], ['参与者', '设计 + 产品'], ['时间', '下周三'], ['议题', '解决按钮误解和筛选保存']] },
          doc: { icon: '📄', label: '文档草稿', lines: [['文档标题', '高级筛选交互优化方案'], ['负责人', '设计'], ['截止', '下周三'], ['问题', '按钮识别度低，疑似付费功能']] }
        }
      },
      {
        id: 4,
        text: '销售两周内跟进客户续费沟通',
        owner: '销售',
        deadline: '两周内',
        defaultTarget: 'cal',
        targets: {
          msg: { icon: '💬', label: '飞书消息', lines: [['接收人', '@销售'], ['内容', '请两周内跟进客户续费沟通，下月到期'], ['发送时间', '立即']] },
          cal: { icon: '📅', label: '日历事件', lines: [['事件', '客户续费沟通'], ['负责人', '销售'], ['时间', '两周内'], ['背景', '客户下月到期']] },
          doc: { icon: '📄', label: '文档草稿', lines: [['文档标题', '客户续费跟进计划'], ['负责人', '销售'], ['截止', '两周内'], ['背景', '本次访谈反馈整体积极']] }
        }
      }
    ]
  },
  {
    label: '跨部门对齐',
    transcript: `市场：下个月要搞一个 AI 产品发布会，我们计划在 20 号，需要产品这边提供核心功能演示素材。
产品：没问题，但演示 demo 需要研发配合提前准备好稳定版本，不能临时搭。
研发：稳定版我们 15 号能 ready，但需要市场提前给我们目标场景清单，不然不知道哪些功能要重点稳定。
市场：好，那我们这周就给。另外活动当天需要一个专属落地页，需要设计支持。
设计：落地页我接，但需要市场给物料清单和产品卖点，最好下周一给到我，不然时间不够。
主持人：总结——市场本周内给研发场景清单，下周一给设计物料清单；研发 15 号前准备好稳定演示版本；产品配合研发准备功能演示素材；设计在收到物料后 5 个工作日内出落地页初稿。`,
    actions: [
      {
        id: 1,
        text: '市场本周内给研发提供演示场景清单',
        owner: '市场',
        deadline: '本周内',
        defaultTarget: 'doc',
        targets: {
          msg: { icon: '💬', label: '飞书消息', lines: [['接收人', '@市场'], ['内容', '请本周内提供 AI 发布会演示场景清单给研发'], ['发送时间', '立即']] },
          cal: { icon: '📅', label: '日历事件', lines: [['事件', '演示场景清单交付'], ['负责人', '市场'], ['截止', '本周五'], ['接收方', '研发']] },
          doc: { icon: '📄', label: '文档草稿', lines: [['文档标题', 'AI 发布会演示场景清单'], ['负责人', '市场'], ['截止', '本周内'], ['用途', '研发据此准备稳定版本']] }
        }
      },
      {
        id: 2,
        text: '市场下周一给设计提供物料清单和产品卖点',
        owner: '市场',
        deadline: '下周一',
        defaultTarget: 'doc',
        targets: {
          msg: { icon: '💬', label: '飞书消息', lines: [['接收人', '@市场'], ['内容', '请下周一前给设计提供物料清单和产品卖点文案'], ['发送时间', '本周五提醒']] },
          cal: { icon: '📅', label: '日历事件', lines: [['事件', '落地页物料交付'], ['负责人', '市场'], ['截止', '下周一'], ['接收方', '设计']] },
          doc: { icon: '📄', label: '文档草稿', lines: [['文档标题', '发布会落地页物料清单'], ['负责人', '市场'], ['截止', '下周一'], ['内容', 'Banner 图、卖点文案、产品截图']] }
        }
      },
      {
        id: 3,
        text: '研发 15 号前准备好发布会稳定演示版本',
        owner: '研发',
        deadline: '15 号',
        defaultTarget: 'cal',
        targets: {
          msg: { icon: '💬', label: '飞书消息', lines: [['接收人', '@研发'], ['内容', '请 15 号前完成演示稳定版本，场景清单由市场本周内提供'], ['发送时间', '立即']] },
          cal: { icon: '📅', label: '日历事件', lines: [['事件', '演示稳定版本 Ready'], ['负责人', '研发'], ['时间', '15 号 EOD'], ['依赖', '市场本周提供场景清单']] },
          doc: { icon: '📄', label: '文档草稿', lines: [['文档标题', '发布会演示版本准备计划'], ['负责人', '研发'], ['截止', '15 号'], ['要求', '按场景清单稳定对应功能']] }
        }
      },
      {
        id: 4,
        text: '设计收到物料后 5 个工作日内出落地页初稿',
        owner: '设计',
        deadline: '收到物料后 5 工作日',
        defaultTarget: 'cal',
        targets: {
          msg: { icon: '💬', label: '飞书消息', lines: [['接收人', '@设计'], ['内容', '收到市场物料后请 5 个工作日内出落地页初稿'], ['触发', '收到物料时']] },
          cal: { icon: '📅', label: '日历事件', lines: [['事件', '落地页初稿评审'], ['负责人', '设计'], ['时间', '收到物料后第 5 工作日'], ['参与者', '设计 + 市场 + 产品']] },
          doc: { icon: '📄', label: '文档草稿', lines: [['文档标题', 'AI 发布会落地页设计方案'], ['负责人', '设计'], ['截止', '物料到位后 5 工作日'], ['交付', 'Figma 初稿']] }
        }
      }
    ]
  }
];

// 当前状态
let currentDemoIdx = 0;
let actionStates = {}; // id -> { status, selectedTarget }

// ══ 初始化 ══
loadDemo(0);

function loadDemo(idx) {
  currentDemoIdx = idx;
  document.getElementById('transcript').value = DEMOS[idx].transcript;
  document.querySelectorAll('.demo-chip').forEach((c, i) => {
    c.classList.toggle('active', i === idx);
  });
}

function goScreen(n) {
  [1,2,3].forEach(i => {
    document.getElementById('screen-' + i).classList.toggle('active', i === n);
    document.getElementById('pill-' + i).classList.toggle('active', i === n);
  });
  window.scrollTo({ top: 0, behavior: 'smooth' });
}

// ══ Screen 1 → Screen 2 ══
function startExtract() {
  const overlay = document.getElementById('extracting');
  overlay.classList.add('show');
  document.querySelector('.s1-actions').style.opacity = '0.4';
  document.querySelector('.s1-actions').style.pointerEvents = 'none';

  setTimeout(() => {
    overlay.classList.remove('show');
    document.querySelector('.s1-actions').style.opacity = '';
    document.querySelector('.s1-actions').style.pointerEvents = '';
    buildActionList();
    goScreen(2);
  }, 1600);
}

function buildActionList() {
  const actions = DEMOS[currentDemoIdx].actions;
  actionStates = {};
  actions.forEach(a => {
    actionStates[a.id] = { status: 'pending', selectedTarget: a.defaultTarget };
  });

  const subtitle = document.getElementById('s2-subtitle');
  subtitle.textContent = `AI 提取了 ${actions.length} 条行动项，逐条确认分发目标`;

  const list = document.getElementById('action-list');
  list.innerHTML = actions.map(a => renderCard(a)).join('');
  updateDispatchCount();
}

function renderCard(a) {
  const state = actionStates[a.id];
  const targets = a.targets;
  const tabsHtml = Object.entries(targets).map(([k, t]) =>
    `<span class="dispatch-tab ${state.selectedTarget === k ? 'active' : ''}" onclick="selectTarget(${a.id}, '${k}')">${t.icon} ${t.label}</span>`
  ).join('');

  const sel = targets[state.selectedTarget];
  const previewHtml = sel.lines.map(([k, v]) =>
    `<div class="pv-line"><span class="pv-key">${k}</span><span class="pv-val">${v}</span></div>`
  ).join('');

  const isConfirmed = state.status === 'confirmed';

  return `
  <div class="action-card status-${state.status}" id="card-${a.id}">
    <div class="action-card-header">
      <div class="action-card-left">
        <span class="action-num">${String(a.id).padStart(2,'0')}</span>
        <div class="action-text" id="text-${a.id}">${a.text}</div>
      </div>
      <span class="status-badge ${state.status === 'confirmed' ? 'confirmed' : 'pending'}">${state.status === 'confirmed' ? '✓ 已确认' : '待确认'}</span>
    </div>

    <div class="dispatch-selector">
      <div class="dispatch-label">分发目标</div>
      <div class="dispatch-tabs">${tabsHtml}</div>
      <div class="preview-block">
        <div class="preview-block-title">${sel.icon} ${sel.label} · 预览</div>
        <div class="preview-content" id="preview-${a.id}">${previewHtml}</div>
      </div>
    </div>

    <div class="action-btns">
      <button class="btn-sm confirm" onclick="confirmCard(${a.id})">✓ 确认</button>
      <button class="btn-sm" onclick="editCard(${a.id})">✎ 修改</button>
      <button class="btn-sm delete" onclick="deleteCard(${a.id})">✗ 删除</button>
    </div>

    <div class="edit-modal" id="edit-${a.id}">
      <textarea id="edittext-${a.id}" rows="2">${a.text}</textarea>
      <div class="edit-modal-actions">
        <button class="btn-sm confirm" onclick="saveEdit(${a.id})">保存</button>
        <button class="btn-sm" onclick="cancelEdit(${a.id})">取消</button>
      </div>
    </div>

    <div class="confirmed-overlay">✓ 已确认 · 将分发至 ${sel.icon} ${sel.label}</div>
  </div>`;
}

function selectTarget(id, key) {
  actionStates[id].selectedTarget = key;
  const demo = DEMOS[currentDemoIdx];
  const a = demo.actions.find(x => x.id === id);
  const card = document.getElementById('card-' + id);

  // 更新 tab 高亮
  card.querySelectorAll('.dispatch-tab').forEach((tab, i) => {
    const keys = Object.keys(a.targets);
    tab.classList.toggle('active', keys[i] === key);
  });

  // 更新预览
  const sel = a.targets[key];
  const previewHtml = sel.lines.map(([k, v]) =>
    `<div class="pv-line"><span class="pv-key">${k}</span><span class="pv-val">${v}</span></div>`
  ).join('');
  document.getElementById('preview-' + id).innerHTML = previewHtml;

  // 更新已确认 overlay 文字
  const overlay = card.querySelector('.confirmed-overlay');
  if (overlay) overlay.textContent = `✓ 已确认 · 将分发至 ${sel.icon} ${sel.label}`;
}

function confirmCard(id) {
  actionStates[id].status = 'confirmed';
  const card = document.getElementById('card-' + id);
  card.className = 'action-card status-confirmed just-confirmed';
  setTimeout(() => card.classList.remove('just-confirmed'), 300);
  card.querySelector('.status-badge').className = 'status-badge confirmed';
  card.querySelector('.status-badge').textContent = '✓ 已确认';
  card.querySelector('.dispatch-selector').style.display = 'none';
  card.querySelector('.action-btns').style.display = 'none';
  const sel = DEMOS[currentDemoIdx].actions.find(a => a.id === id).targets[actionStates[id].selectedTarget];
  card.querySelector('.confirmed-overlay').style.display = 'block';
  card.querySelector('.confirmed-overlay').textContent = `✓ 已确认 · 将分发至 ${sel.icon} ${sel.label}`;
  updateDispatchCount();
}

function editCard(id) {
  const modal = document.getElementById('edit-' + id);
  modal.classList.toggle('show');
}

function saveEdit(id) {
  const newText = document.getElementById('edittext-' + id).value.trim();
  if (newText) {
    document.getElementById('text-' + id).textContent = newText;
    DEMOS[currentDemoIdx].actions.find(a => a.id === id).text = newText;
  }
  document.getElementById('edit-' + id).classList.remove('show');
}

function cancelEdit(id) {
  document.getElementById('edit-' + id).classList.remove('show');
}

function deleteCard(id) {
  actionStates[id].status = 'deleted';
  const card = document.getElementById('card-' + id);
  card.style.transition = 'opacity 0.3s, transform 0.3s';
  card.style.opacity = '0';
  card.style.transform = 'scale(0.97)';
  setTimeout(() => {
    card.style.display = 'none';
    updateDispatchCount();
  }, 300);
}

function confirmAll() {
  const actions = DEMOS[currentDemoIdx].actions;
  actions.forEach(a => {
    if (actionStates[a.id].status === 'pending') {
      confirmCard(a.id);
    }
  });
}

function updateDispatchCount() {
  const actions = DEMOS[currentDemoIdx].actions;
  const confirmed = actions.filter(a => actionStates[a.id] && actionStates[a.id].status === 'confirmed').length;
  const total = actions.filter(a => actionStates[a.id] && actionStates[a.id].status !== 'deleted').length;
  document.getElementById('dispatch-count').innerHTML = `已确认 <strong>${confirmed}</strong> / ${total} 条`;
  const btn = document.getElementById('dispatch-btn');
  btn.disabled = confirmed === 0;
}

// ══ Screen 2 → Screen 3 ══
function doDispatch() {
  const actions = DEMOS[currentDemoIdx].actions;
  const confirmed = actions.filter(a => actionStates[a.id] && actionStates[a.id].status === 'confirmed');

  document.getElementById('s3-subtitle').textContent = `${confirmed.length} 条行动项已成功分发`;

  const icons = { msg: '💬', cal: '📅', doc: '📄' };
  const destLabels = { msg: '飞书消息', cal: '日历事件', doc: '飞书文档草稿' };

  const resultHtml = confirmed.map(a => {
    const tKey = actionStates[a.id].selectedTarget;
    const t = a.targets[tKey];
    const destName = t.lines[0]?.[1] || t.label;
    return `
    <div class="result-item">
      <div class="result-icon">${t.icon}</div>
      <div class="result-body">
        <div class="result-action">${a.text}</div>
        <div class="result-dest">→ ${destLabels[tKey]}：${destName}</div>
      </div>
      <div class="result-status">已发送</div>
    </div>`;
  }).join('');

  document.getElementById('result-list').innerHTML = resultHtml;
  goScreen(3);
}

function resetAll() {
  loadDemo(0);
  goScreen(1);
}
</script>
</body>
</html>
